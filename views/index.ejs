<!DOCTYPE html>

<html lang="FR">
    <head>
        <title>My todolist</title>
        <meta charset="utf-8" />
        <meta name='viewport' content='width=device-width . initial-scale=1.0 . maximum-scale=1.0' />

        <!-- Style CSS -->
        <link rel="stylesheet" href="composants/css/main.css" />
    </head>
        <body>

                <!-- || BEGIN || Todo List Div-->

            <div>
                <h1>My Todo List</h1>

                <ul>
                <% todolist.forEach(function(todo, index) { %>
                    <li><a href="/supprimer/<%= index %>">✘</a> <%= todo %></li>
                <% }); %>
                </ul>

                <form action="/ajouter/" method="post">
                    <p>
                        <label for="newtodo">To do</label><br><br>
                        <input type="text" name="newtodo" id="newtodo" autofocus />
                        <input type="submit" />
                    </p>
                </form>
            </div>

                    <!-- || END || Todo List Div-->


        <!-- Scripts -->
        <script src="composants/js/jquery.min.js"></script>
        </body>
</html>